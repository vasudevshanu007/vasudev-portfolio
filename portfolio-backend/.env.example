# Copy this file to .env and fill in your values.
# NEVER commit .env to version control!

# Gmail credentials — use an App Password, not your real password.
# Generate one at: https://myaccount.google.com/apppasswords
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_gmail_app_password

# Comma-separated list of allowed frontend origins (leave empty to allow all)
# Example: ALLOWED_ORIGINS=https://yourportfolio.vercel.app,https://yourportfolio.netlify.app
ALLOWED_ORIGINS=

# Server port (default 5000)
PORT=5000
